# Photoshop BatchPlay

Use this note for BatchPlay planning and validation.

## Principles

- Keep `execute` disabled by default.
- Validate descriptor shape and risk first.
- Use typed wrappers and schema checks before allowing future execution.
- Prefer UXP-side controlled execution over raw arbitrary payload passthrough.
