# Photoshop Export

Use this note for preview and deliverable export planning.

## Principles

- Only write into `sandbox/` or `output/`.
- Default to `dry_run=true`.
- Generate an EvidenceManifest for every preview iteration.
- Do not overwrite user PSD assets or export over real deliverables.
